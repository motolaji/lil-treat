Create a logout modal that should reuse the overlay modal that we already have
If a user taps the logout button either on the sidebar or the profile detail page
it should pop-up to confirm. In the content, it should show how many treats the user
has collected across the different vendors. It should display a minified version of the
metrics shown on the profile detail page. Minified here means the black card is made
a bit smaller as well as the content inside to fit into the modal well.

It should then highlight to the user that logging out will mean any new treats they collect
will not be added to the treats for this account?

It should have two buttons "Back to App" and "Logout".

The title of the modal should be logout