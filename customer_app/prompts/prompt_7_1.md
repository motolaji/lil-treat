Visual Feedback items:

1. On the VendorScreen, add more padding in the body. The cards are too wide
currently
2. Remove the green highlight around the vendor logo on the VendorScreen
3. You correctly linked the card from the homescreen and the treat jar page
to the VendorScreen. However, the parts of the card that send the user to the
page is currently just the button. Change this so the entire card is tappable
and sends the user to the vendor screen
4. On the VendorScreen, add an underline to the sub-tab that is currently active
5. On the TreatJar screen there is still a green outline on the input search field.
Change this to black. And more generally, extract the style that indicates the
field is active to the main TextField component so any page that uses it gets
that behaviour for free and consistently